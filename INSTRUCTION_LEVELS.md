# Изменение порогов уровней — ИТОГОВАЯ ИНСТРУКЦИЯ / ОТЧЁТ (обновлено)

## Правило
Каждая 2-я победа = новый уровень. Уровней 15, максимум: 28 побед.
Формула: `currentLevel = min(15, pobed/2 + 1)`

| Уровень | Победы (pobed) | nextLevel (порог след. уровня) |
|---|---|---|
| 1  | 0–1  | 2  |
| 2  | 2–3  | 4  |
| 3  | 4–5  | 6  |
| 4  | 6–7  | 8  |
| 5  | 8–9  | 10 |
| 6  | 10–11| 12 |
| 7  | 12–13| 14 |
| 8  | 14–15| 16 |
| 9  | 16–17| 18 |
| 10 | 18–19| 20 |
| 11 | 20–21| 22 |
| 12 | 22–23| 24 |
| 13 | 24–25| 26 |
| 14 | 26–27| 28 |
| 15 | 28+  | 30 |

## Что изменено (ВЫПОЛНЕНО и проверено симулятором байткода)

### 1. Podkidnoj.smali — метод switchLevel()
Цепочка блоков :cond_1..:cond_14 перегенерирована по таблице выше.
Симуляция pobed=0..119 → уровень всегда = min(15, pobed/2+1). OK.

### 2. GameMenuTel.smali — метод switchLevel()
Аналогично перегенерирован (то же поле pobed сохраняется в shared prefs
и восстанавливается на этом экране). Фото соперника перед игрой выбирается
через packed-switch 0x1..0x12 по currentLevel (строки ~623 и ~946),
поэтому синхронизируется автоматически. Старые лишние уровни 16–18
в порогах убраны. OK.

### 3. GameMenuMain.smali — метод switchGirls() (главный экран, 6 фото girlsFinish1–6)
Старая логика с неровными порогами (1-5, 5-10, ... до 62 побед) заменена
на 3 блока, синхронных с уровнями:
- pobed 0..27  (уровни 1–14):  позы (9, 9, 9, 9, 12, 15)
- pobed 28..35 (уровень 15):   позы (11, 11, 8, 8, 14, 17)
- pobed >= 36  (финал/все пройден): позы (9, 3, 6, 1, 0, 0)
Вызовы Girls.girls(I)V сохранены как в оригинале.

## Важные замечания для пересборки
1. Поле nextLevel записывается во всех ветках (для совместимости с
   оригинальной схемой), но вся логика отображения опирается на
   currentLevel — именно он теперь меняется каждые 2 победы.
2. .locals 2 не менялся; используются только v0, v1 (регистры v2–v6 из
   старой версии switchGirls больше не нужны — метод полностью заменён).
3. Метки :cond_N все достижимы, висящих меток нет — smaliassm/baksmali
   сборка не должна ругаться.
4. Бэкапы оригиналов: *.smali.bak (Podkidnoj, GameMenuTel, GameMenuMain).
5. Сборка: apktool b <распакованный_каталог> -o new.apk, затем подпись
   (apksigner sign --ks your.keystore new.apk).

## Если нужно подправить пороги фото главного экрана
Константы поз лежат в методе switchGirls() GameMenuMain.smali — три блока
по 6 вызовов invoke-virtual {v0, v1}, Girls;->girls(I)V. Меняются только
значения const после iget-object girlsFinishN.
