# ИНСТРУКЦИЯ ДЛЯ ИИ: Изменение порогов уровней в игре "Дурак" (Podkidnoj)

## ЦЕЛЬ ЗАДАЧИ
Изменить прогрессию побед так, чтобы **переход на следующий уровень происходил каждые 2 победы**.
Всего в игре **15 уровней** (currentLevel = 1..15). Новый уровень N начинается при `pobed >= 2*N - 1`.

Новая таблица порогов (уровень → диапазон побед → следующий порог):

| currentLevel | pobed (диапазон) | nextLevel (порог след. уровня) |
|---|---|---|
| 1  | 1          | 3  |
| 2  | 2 – 3      | 5  |
| 3  | 4 – 5      | 7  |
| 4  | 6 – 7      | 9  |
| 5  | 8 – 9      | 11 |
| 6  | 10 – 11    | 13 |
| 7  | 12 – 13    | 15 |
| 8  | 14 – 15    | 17 |
| 9  | 16 – 17    | 19 |
| 10 | 18 – 19    | 21 |
| 11 | 20 – 21    | 23 |
| 12 | 22 – 23    | 25 |
| 13 | 24 – 25    | 27 |
| 14 | 26 – 27    | 29 |
| 15 | 28 и более | (30 — финальный, дальше не растёт) |

Старые пороги были нерегулярными: 1, 2-4, 5, ..., с шагом 2 только местами и «плато» по 4-6 побед (см. ниже текущие константы).

Формула для любого уровня N (N от 2 до 14):
- нижняя граница: `2*N - 2`
- верхняя граница (исключительно): `2*N`
- nextLevel: `2*N + 1`... Нет, аккуратнее: уровень N активен при `pobed >= 2*(N-1)+1` и `pobed < 2*N+1`, nextLevel = `2*N+1`.
Проверка: N=2 → pobed 2..3 (>=3? нет). Поэтому используйте таблицу выше — она точная, её и внедряйте, формулы применять не нужно.

---

## ФАЙЛ 1: `NardeNoAndroid/Podkidnoj.smali` — метод `switchLevel()`

Метод находится примерно на **строках 6481–6735** (начинается с `.method public switchLevel()V`).
Это цепочка if/else блоков над полем `iget v0, p0, Lsk/mladyumelec/narde/Podkidnoj;->pobed:I`.

### Текущая структура метода (шаблоны):
Первый блок (без изменений — оставить как есть):
```smali
    iget v0, p0, Lsk/mladyumelec/narde/Podkidnoj;->pobed:I
    const/4 v1, 0x1
    if-lt v0, v1, :cond_0
    iput v1, p0, ...;->currentLevel:I     # level=1
    const/4 v1, 0x2
    iput v1, p0, ...;->nextLevel:I        # next=2
    goto :goto_0
```
Каждый следующий блок `:cond_N` выглядит так (пример cond_1, уровень 2):
```smali
    :cond_1
    iget v0, p0, Lsk/mladyumelec/narde/Podkidnoj;->pobed:I
        const/4 v0, 0x2          # НИЖНЯЯ граница диапазона
    if-lt v0, v1, :cond_2
    iget v0, p0, Lsk/mladyumelec/narde/Podkidnoj;->pobed:I
        const/4 v0, 0x4          # ВЕРХНЯЯ граница (исключительно)
    if-ge v0, v1, :cond_2
    const/4 v0, 0x2              # currentLevel
    iput v0, p0, ...;->currentLevel:I
    const/4 v0, 0x4              # nextLevel
    iput v0, p0, ...;->nextLevel:I
    goto :goto_0
```
Последний блок `:cond_14` (уровень 15):
```smali
    :cond_14
    const/16 v1, 0x1c            # нижняя граница (28) — МЕНЯТЬ НЕ НУЖНО, уже 0x1c
    if-lt v0, v1, :cond_0
    const/16 v0, 0xf             # currentLevel=15
    iput v0, p0, ...;->currentLevel:I
    const/16 v0, 0x1e            # nextLevel=30
    iput v0, p0, ...;->nextLevel:I
```

### Что менять (константы порогов pobed) — всего 14 блоков cond_1..cond_14:
Для каждого блока меняются ТРИ числа: нижняя граница, верхняя граница и nextLevel
(число `currentLevel` — 2,3,4,...,14 — ОСТАВЛЯЕМ без изменений).

| Блок | нижняя граница было→стало | верхняя граница было→стало | nextLevel было→стало |
|---|---|---|---|
| :cond_1  (lv2)  | 0x2 → 0x2  (без изм.) | 0x4 → 0x4 (без изм.) | 0x4 → 0x4 (без изм.) |
| :cond_2  (lv3)  | 0x4 → 0x4  | 0x6 → 0x6  | 0x6 → 0x6  (без изм.) |
| :cond_3  (lv4)  | 0x6 → 0x6  | 0x8 → 0x8  | 0x8 → 0x8  (без изм.) |
| :cond_4  (lv5)  | 0x8 → 0x8  | 0xa → 0xa  | 0xa → 0xa  (без изм.) |
| :cond_5  (lv6)  | 0xa → 0xa  | 0xc → 0xc  | 0xc → 0xc  (без изм.) |
| :cond_6  (lv7)  | 0xc → 0xc  | 0xe → 0xe  | 0xe → 0xe  (без изм.) |
| :cond_7  (lv8)  | 0xe → 0xe  | 0x10 → 0x10| 0x10 → 0x10(без изм.) |
| :cond_8  (lv9)  | 0x10 → 0x10| 0x12 → 0x12| 0x12 → 0x12(без изм.) |
| :cond_9  (lv10) | 0x12 → 0x12| 0x14 → 0x14| 0x14 → 0x14(без изм.) |
| :cond_10 (lv11) | 0x14 → 0x14| 0x16 → 0x16| 0x16 → 0x16(без изм.) |
| :cond_11 (lv12) | 0x16 → 0x16| 0x18 → 0x18| 0x18 → 0x18(без изм.) |
| :cond_12 (lv13) | 0x18 → 0x18| 0x1a → 0x1a| 0x1a → 0x1a(без изм.) |
| :cond_13 (lv14) | 0x1a → 0x1a| 0x1c → 0x1c| 0x1c → 0x1c(без изм.) |
| :cond_14 (lv15) | 0x1c → 0x1c| —          | 0x1e → 0x1e(без изм.) |

⚠️ ВНИМАНИЕ: проверьте фактические значения в файле перед правкой! В текущей версии Podkidnoj.smali блоки
уже идут с шагом 2 (2,4 / 4,6 / 6,8 / 8,10 / ... / 26,28 / 28+), т.е. **правило «каждая 2-я победа — новый уровень»
там уже выполнено**, начиная с 2 побед. Убедитесь, что первый переход (level 1 → 2) происходит при 1 победе
(`const/4 v1, 0x1` в первом блоке) — это соответствует таблице (уровень 2 начинается с pobed=2, а уровень 1 — при 1 победе).
Если в файле обнаружены другие пороги (например, 0xd, 0x10, 0x16, 0x1a... с неровным шагом) — приведите их к таблице выше.

Также убедитесь, что после `:cond_14` нет «лишних» плато: последний порог 0x1c (28) и nextLevel 0x1e (30).

---

## ФАЙЛ 2: `NardeNoAndroid/GameMenuTel.smali` — экран выбора соперника (фото до начала игры)

### 2.1. Метод `switchLevel()` (строки ~1782–2035)
Полный клон метода из Podkidnoj.smali (тот же шаблон, класс `GameMenuTel`).
Применить ту же таблицу порогов, что и в Файле 1. Сейчас там тоже шаги 2,4/4,6/6,8/8,10/10,12/12,14/14,16...
— сверить и привести к единой таблице (каждая 2-я победа = новый уровень).

### 2.2. Фото соперника — `sozdaemGirls()` (~строка 621)
```smali
    iget v0, p0, Lsk/mladyumelec/narde/GameMenuTel;->currentLevel:I
    packed-switch v0, :pswitch_data_0
```
Таблица `.packed-switch 0x1` (~строка 947) содержит метки `:pswitch_0 .. :pswitch_11` (уровни 1–12) + default.
**МЕНЯТЬ НЕ ТРЕБУЕТСЯ**: фото выбирается по `currentLevel`, а не по `pobed`. Как только `switchLevel()`
выше будет корректным, фото автоматически будет соответствовать уровню. Проверить только, что
`:pswitch_data_0` покрывает уровни 1..15 (если packed-switch заканчивается на 0xc — досмотреть default-ветку).

---

## ФАЙЛ 3: `NardeNoAndroid/GameMenuMain.smali` — главный экран (6 фото girlsFinish1..6)

### 3.1. Метод `switchGirls()` (строки ~979–1875)
Цепочка блоков по полю `iget v0, p0, Lsk/mladyumelec/narde/GameMenuMain;->pobed:I`
(метки `:cond_1 ... :cond_11`, вызывается из `onResume`/`show`, см. строку 479).

Текущие границы pobed в этом методе (строки с константами):
- cond_1: `>=1, <5`  (`const/4 v1, 0x1` / `if-ge v0, v4` где `v4 = 0x5`)
- далее пары порогов: 0xa(10), 0xd(13), 0x10(16), 0x12(18), 0x16(22), 0x1a(26), 0x1e(30), 0x20(32), 0x24(36), 0x28(40)... 0x3c(60), 0x3e(62)

Это СТАРЫЕ неровные пороги (шаг 3,4,5,6 побед). Их нужно заменить на шаг 2 согласно таблице уровней:

| Блок | было (диапазон pobed) | стало |
|---|---|---|
| before cond_1 | < 1 | < 1 (без изм.) |
| cond_1  | 1 … <5 (v4=0x5) | 1 … <4 → заменить `if-ge v0, v4` на `const/4 v1, 0x4` + `if-ge v0, v1` |
| cond_2  | 5 … <10 (0xa)   | 4 … <6  (верх. граница 0x6) |
| cond_3  | 10 … <13 (0xd)  | 6 … <8  (0x8) |
| cond_4  | 13 … <16 (0x10) | 8 … <10 (0xa) |
| cond_5  | 16 … <18 (0x12) | 10 … <12 (0xc) |
| cond_6  | 18 … <22 (0x16) | 12 … <14 (0xe) |
| cond_7  | 22 … <26 (0x1a) | 14 … <16 (0x10) |
| cond_8  | 26 … <30 (0x1e) | 16 … <18 (0x12) |
| cond_9  | 30 … <32 (0x20) | 18 … <20 (0x14) |
| cond_10 | 32 … <36 (0x24) | 20 … <22 (0x16) |
| cond_11 | 36 … <40 (0x28) и далее до 0x3c/0x3e | 22 … <24, 24 … <26, 26 … <28, 28+ |

Внутри каждого блока set-вызовы `girlsFinishN.girls(I)` (номера картинок 0x9, 0x3, 0x6, ...) — НЕ трогать,
меняться должны ТОЛЬКО числа в условиях `if-lt/if-ge` (пороги pobed). Количество картинок/фото (6 штук на экране)
и сами номера фотографий оставить как есть — меняется только скорость их смены.

Если блоков cond в switchGirls больше, чем 15 диапазонов (файл содержит блоки вплоть до pobed=0x3e=62) —
объедините все блоки с pobed ≥ 28 в один финальный (уровень 15, картинка max), либо сохраните их как бонусные
«плато» после 28 побед — но тогда правило «каждая 2-я победа» действует до 28 побед. Рекомендуемый вариант:
последовательные блоки с шагом 2 вплоть до 0x1c (28), всё что ≥28 — финальная картинка.

### 3.2. Важно про регистры
В начале метода заняты кэшированные константы: `v2=0x2, v3=0xf, v4=0x5, v5=0xc, v6=0x8`.
Они используются внутри блоков как аргументы `girls(I)` (номера фото) — не удалять.
При замене порогов используйте локальную перезагрузку `const/16 v1, 0xNN` прямо перед сравнением
(как это уже сделано в существующих блоках: `iget v0 ...; const/16 v1, 0xa; if-lt v0, v1, :cond_N`).

---

## ПОРЯДОК ДЕЙСТВИЙ
1. Сделать резервные копии (уже есть: *.smali.bak).
2. Отредактировать `switchLevel()` в Podkidnoj.smali (Файл 1).
3. Отредактировать `switchLevel()` в GameMenuTel.smali (Файл 2.1).
4. Отредактировать пороги pobed в `switchGirls()` GameMenuMain.smali (Файл 3.1).
5. Проверить синтаксис smali: метки :cond_N на месте, goto :goto_0, .end method; все `if-lt/if-ge`
   используют определнные регистры; количество веток не изменилось.
6. Пересобрать APK: `apktool b NardeNoAndroid -o game-new.apk`, подписать (apksigner / jarsigner debug-ключом).
7. Протестировать: 1 победа → level 2, затем каждая 2-я победа → +1 уровень; фото на главном экране
   и фото соперника меняются синхронно с уровнем.

## КОНТРОЛЬНЫЕ ТОЧКИ ДЛЯ ТЕСТА
- pobed=1 → currentLevel=1 (главный экран: набор фото блока cond_1)
- pobed=2 → currentLevel=2 (следующий набор фото, другой соперник)
- pobed=4 → currentLevel=3
- pobed=28 → currentLevel=15 (максимум, дальше уровни не растут)
